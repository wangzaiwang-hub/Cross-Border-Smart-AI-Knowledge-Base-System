# 2026-07-13 集中验收记录

## 验收结论

本轮完成后端全 Reactor、前端生产构建、四套部署编排和低配置场景化运行验收。所有代码变更均已按独立提交推送到 GitHub 功能分支。

## 自动化门禁

| 门禁 | 命令 | 结果 |
|---|---|---|
| 后端全量验证 | `.\mvnw.cmd clean verify` | 53 个 Reactor 模块全部成功，耗时 10 分 47 秒 |
| 前端类型检查 | `pnpm type-check` | shared、mall、admin 全部通过 |
| 前端生产构建 | `pnpm build` | mall、admin 全部成功 |
| constrained VM Compose | `docker compose ... vm-compose.yml config --quiet` | 通过 |
| constrained local Compose | `docker compose ... local-compose.yml config --quiet` | 通过 |
| constrained apps Compose | `docker compose ... apps-compose.yml config --quiet` | 通过 |
| enterprise Compose | `docker compose ... enterprise-server/compose.yml config --quiet` | 通过 |
| CycloneDX SBOM | `.\mvnw.cmd -Prelease-sbom package -DskipTests` | 生成 BOM 1.6，聚合 493 个组件 |

## 真实场景验证

1. 平台链路：Gateway、Auth、User、System、Admin 健康；注册、登录、JWT、默认用户档案链路通过。
2. 商城链路：商品发布、库存、虚拟充值、订单预览/创建、钱包支付、RocketMQ 支付事件、模拟履约完成；订单最终为 `COMPLETED`。
3. 知识链路：TXT 安全上传、解析、审核发布、PGVector/Elasticsearch 索引重试恢复；索引任务最终为 `SUCCEEDED`。
4. AI 链路：无豆包密钥时使用显式 fail-closed 适配器，不伪造模型回答；数据库与检索链路可运行。真实豆包生成效果需注入外部 Secret 后验收。
5. 培训链路：课程、章节、关卡、题目、发布、员工分配、服务端心跳、测验评分全部通过；最终 `COMPLETED / 100% / 100分`。
6. 通知链路：培训事件通过 RocketMQ 生成 2 条站内信，单条已读与全部已读使未读数 `2 → 1 → 0`。
7. 非 root：培训和通知容器内 Java 进程实测以 UID `10001` 运行。

## 本轮运行时缺陷及修复

- Spring Boot 4 模块化自动配置下，AI、Search、Notification 缺少 `spring-boot-starter-flyway`，导致服务健康但业务表未迁移；已补齐并验证真实数据库迁移。
- Elasticsearch 开启安全认证而 Search 客户端未携带 Basic Auth；已统一安全客户端工厂并覆盖索引、检索、删除和版本切换。
- 培训 `MAX(score)` 在真实 MySQL 返回 `Number` 的具体类型与测试假设不一致；已改为 `Number.intValue()`。
- Docker Desktop 3GB 环境不能同时常驻 Elasticsearch、RocketMQ 和全部 Java 服务；已验证严格按平台、商城、AI、培训/通知场景互斥运行。

## 尚未关闭的发布项

- 未提供豆包 API Key，不能声称真实大模型回答质量已经通过；密钥必须经环境变量或 Secret 注入。
- 前端构建成功，但 Element Plus/ECharts 主 chunk 超过 500KB，正式发布前需要 manualChunks 和按需加载优化。
- CycloneDX 报告保留旧 Javassist `3.21.0-GA` 传递依赖风险，需在漏洞/许可证审计中形成接受或升级记录。
- 浏览器级视觉回归、性能 P95、备份恢复演练、最终 PR 合并与版本标签仍属于发布阶段门禁。
