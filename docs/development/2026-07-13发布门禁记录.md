# 2026-07-13 发布门禁记录

## 已通过项

- Maven：`mvnw.cmd clean verify` 完整通过 58 个 Reactor 模块，耗时 664.8 秒。
- 测试报告：139 份 Surefire XML，364 项测试，0 失败，0 错误，1 跳过。跳过项为默认关闭的外部 Seata 运行测试；该测试已在真实 Seata 2.5.0 + JDK 25 环境独立启用并通过。
- 前端：`pnpm type-check` 和 `pnpm build` 通过；Playwright 商城 12 路由、管理端 8 路由视觉冒烟通过，无空白页、意外 404 或浏览器异常。视觉冒烟仅对登录边界使用本地模拟，不代替后端真实 E2E。
- Compose：虚拟机 core/ai-data、本机 mall-deps/ai-deps、分组 Java 应用、企业服务器四套编排均通过 `docker compose config`。
- 安全：OWASP ZAP Auth API 扫描 P0/P1 为 0；跟踪文件不含 `.env`、私钥、日志、备份、构建目录和典型密钥格式。
- 许可证：CycloneDX SBOM 含 498 个组件，禁止许可证为 0，19 项保留人工复核记录。
- 性能：查询 P95 93.84ms、写入 P95 43.34ms、商品搜索 P95 103.85ms，错误率均为 0。
- 恢复：MySQL、PGVector、附件隔离恢复，容器重启，消息重放和索引重建均有验收证据。

## 尚未解除的发布条件

1. 未注入真实豆包 API Key/Endpoint ID，因此 AI 引用回答、流式输出和拒答准确率的真模型 E2E 仍未执行。无密钥时系统已验证失败关闭，不伪造回答。
2. `wang@192.168.154.10` 的 SSH 免密仍被虚拟机拒绝；数据库端口可达，已通过只读远程导出完成隔离恢复演练。
3. 在上述 AI E2E 通过前，不签署最终 RC，不合并 `main`，不创建正式版本标签。
